export const selectRefactoredData = state => state.data.refactoredData;

export const selectIsRefactoring = state => state.data.isRefactoring;

export const selectIndividualAndTeamData = state => state.data.individualAndTeamData;

export const selectIndividualRefactoredData = state => state.data.isRefactoredData;

export const selectIsPortfolioRefactoredData = state => state.data.isPortfolioRefactoredData;

export const selectPortfolioRefactoredData = state => state.data.portfolioRefactoredData;

export const selectPortfolioData = state => state.data.portfolioData;

export const selectIsPortfolioData = state => state.data.isPortfolioData;

export const selectIsLoading = state => state.data.isLoading;
